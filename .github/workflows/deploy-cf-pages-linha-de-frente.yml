name: Deploy linha-de-frente → Cloudflare Pages

on:
  push:
    branches:
      - deploy/cf-pages-linha-de-frente
    paths:
      - "web/apps/linha-de-frente/**"
      - "web/packages/**"
      - "web/package.json"
      - "web/turbo.json"
      - ".github/workflows/deploy-cf-pages-linha-de-frente.yml"
  workflow_dispatch: {}

concurrency:
  group: cf-pages-linha-de-frente
  cancel-in-progress: false

permissions:
  contents: read
  deployments: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: web/package-lock.json

      - name: Install deps
        working-directory: web
        # Troca pra `npm ci` quando o repo passar a commitar package-lock.json.
        run: npm install --no-audit --no-fund

      - name: Lint (informativo, não bloqueia deploy)
        working-directory: web
        continue-on-error: true
        run: npx turbo run lint --filter=@pedireito/linha-de-frente

      - name: Typecheck
        working-directory: web
        run: npx turbo run typecheck --filter=@pedireito/linha-de-frente

      - name: Test
        working-directory: web
        run: npx turbo run test --filter=@pedireito/linha-de-frente

      - name: Build
        working-directory: web
        env:
          VITE_TURNSTILE_SITEKEY: ${{ secrets.VITE_TURNSTILE_SITEKEY }}
        run: npx turbo run build --filter=@pedireito/linha-de-frente

      - name: Verify dist artifacts
        run: |
          test -f web/apps/linha-de-frente/dist/index.html
          test -f web/apps/linha-de-frente/dist/_headers

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          command: pages deploy web/apps/linha-de-frente/dist --project-name=linha-de-frente --branch=production
