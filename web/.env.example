# =============================================================================
# Variáveis compartilhadas entre todos os apps do monorepo.
# Na Vercel: configure por projeto em Settings > Environment Variables.
# Local: copie para .env (nunca commitar valores reais).
# =============================================================================

# --- Supabase (Pé Direito) ---
# Servidor (Vercel Functions) — usadas por packages/db/server.ts
SUPABASE_URL=https://fmuhpgrmvawcjezhflmz.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Frontend (Vite expõe as VITE_*) — usadas por packages/db/client.ts
VITE_SUPABASE_URL=https://fmuhpgrmvawcjezhflmz.supabase.co
VITE_SUPABASE_ANON_KEY=

# --- ActiveCampaign (opcional) ---
AC_API_URL=
AC_API_KEY=
AC_LIST_ID=
AC_CUSTOM_FIELD_ID=

# --- Lançamento (lp-lancamento) ---
# Link do convite do grupo oficial WhatsApp (chat.whatsapp.com/...)
VITE_WHATSAPP_INVITE_URL=
